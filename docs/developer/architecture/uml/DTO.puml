This file is obsolete.

Earlier versions of Kitodo.Production used a layer of data transfer objects
(DTOs, BaseDTO, *DTO) between the search index and the frontend. This layer
was removed; the forms and services work with the entity classes directly.

See the section "Transfer objects" in ../README.md.
